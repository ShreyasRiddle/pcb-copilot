/** Returned by `/api/designs` when cloud persistence is not configured (shared with client fallback). */
export const SAVED_DESIGNS_503_MESSAGE =
  "Saved designs are not configured on this server.";
